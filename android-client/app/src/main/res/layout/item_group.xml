<?xml version="1.0" encoding="utf-8"?>
<!--
    Single row in the groups RecyclerView.
    Matches the grid layout from doc 1:
      col 4: permissions | col 2: user | col 2: size | col 4: #IDENTIFIER (right-aligned)
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="8dp"
    android:paddingVertical="14dp">

    <!-- Permissions (weight 4) -->
    <TextView
        android:id="@+id/tvPermissions"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="4"
        android:text="drwxr-xr-x"
        android:textSize="12sp"
        android:textColor="@color/on_surface_variant"
        android:fontFamily="monospace" />

    <!-- User (weight 2) -->
    <TextView
        android:id="@+id/tvUser"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:text="root"
        android:textSize="12sp"
        android:textColor="@color/on_surface_variant"
        android:fontFamily="monospace" />

    <!-- Size (weight 2) -->
    <TextView
        android:id="@+id/tvSize"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:text="4096"
        android:textSize="12sp"
        android:textColor="@color/on_surface_variant"
        android:fontFamily="monospace" />

    <!-- #Identifier (weight 4, right-aligned, white bloom) -->
    <TextView
        android:id="@+id/tvIdentifier"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="4"
        android:text="#GENERAL"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@color/primary_white"
        android:fontFamily="monospace"
        android:gravity="end" />

</LinearLayout>
